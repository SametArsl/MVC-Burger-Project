@model MVC_Burger_Project.ModelVM.BurgerVM

@{
    ViewData["Title"] = "Edit";
}
<div class="row" style="margin-top:200px">
    <div class="container col-md-4">
        <h1 class="text-center">Edit</h1>
        <h4 class="text-center">Edit Selected Sauce Information</h4>
        <hr />
        <form asp-action="Edit" style="margin-top:50px">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Sauce.ID" />
            <div class="form-group">
                <label asp-for="Sauce.Name" class="control-label"></label>
                <input asp-for="Sauce.Name" class="form-control" />
                <span asp-validation-for="Sauce.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Sauce.Picture" class="control-label"></label>
                <input asp-for="Sauce.Picture" class="form-control" type="file" />
                <span asp-validation-for="Sauce.Picture" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Sauce.Price" class="control-label"></label>
                <input asp-for="Sauce.Price" class="form-control" />
                <span asp-validation-for="Sauce.Price" class="text-danger"></span>
            </div>

            <div class="form-group mt-3 text-center" style="margin-bottom:320px">
                <input type="submit" value="Save" class="btn btn-primary" />
                <a asp-action="Index" class="btn btn-outline-dark text-center" style="margin-left:20px">Back to List</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
